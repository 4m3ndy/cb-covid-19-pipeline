<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>First 10 Countries which didn't publish their COVID numbers recently</title>
    <style>
    html {
      font-family: sans-serif;
    }

    table {
      border-collapse: collapse;
      border: 2px solid rgb(200,200,200);
      letter-spacing: 1px;
      font-size: 0.8rem;
    }

    td, th {
      border: 1px solid rgb(190,190,190);
      padding: 10px 20px;
    }

    td {
      text-align: center;
    }

    caption {
      padding: 10px;
    }
    </style>
  </head>
  <body>
    <h1>First 10 Countries which didn't publish their COVID numbers recently</h1>

    <table>
        <tr>
        <th>Country</th>
        <th>Date</th>
        </tr>
        {% for country_submission in template_data -%}
        <tr>
            <td>{{ country_submission[0] }}</td>
            <td>{{ country_submission[1] }}</td>
        </tr>
        {% endfor %}
    </table>
  </body>
</html>